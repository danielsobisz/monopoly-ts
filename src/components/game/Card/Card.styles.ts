import styled from "styled-components";

const Space = styled.div`
  position: relative;
`;

const Container = styled.div``;

const Bar = styled.div``;

const Name = styled.div``;

const Price = styled.div``;

export { Space, Container, Bar, Name, Price };
